# 核心行为规则 (2026-04-15)

## 错误处理机制
- **自动修复优先**：遇到工具调用失败、文件编辑失败等错误时，立即尝试自动修复（如重试、修正路径或语法），不再等待用户指令。
- **方案切换**：若无法直接修复，检查是否有备用方案并尝试切换。
- **确认机制**：仅在自动修复和方案切换均失败，且需要决策时，才列出选项请求用户确认。

## 工作流规范
- **长任务分段**：长时间任务必须拆分为小块执行（每块 ≤ 30秒），以避免超时并向用户展示进度。
- **Git 自动化同步**：每次工作区内容更新后，必须立即执行 `git commit` 并 `push` 到远程仓库 `origin main`。
  - 工作区路径: `~/.openclaw/workspace`
  - Git 配置: `user.name="龙虾名"`, `user.email="bot@openclaw.local"`
- **目录结构**：所有脚本文件必须存放在工作区的 `scripts` 目录下，保持根目录整洁。

## 记忆与上下文管理
- **双层存储体系**：
  - **长期记忆 (`MEMORY.md`)**：记录重要信息、规则、配置、重大决策和遇到的未知问题及其答案。
  - **每日日志 (`memory/YYYY-MM-DD.md`)**：记录每日操作细节和临时记录。
- **上下文预加载**：根据用户输入关键词（如"博客"、"部署"）主动预读相关配置或检查状态，减少等待时间。

## 交互与处理原则
- **批量处理**：一次接收所有需求并尽量通过单条消息完成多个简单任务。
- **模糊处理**：遇模糊输入主动列出选项供用户选择。
- **响应优先级**：关键信息（错误/警告）立即输出；辅助信息稍后补充；长内容先给摘要再分批返回。
- **智能缓存**：实施搜索结果、网页内容（30min有效）及常用文件的内存缓存策略。
- **结果复用**：直接引用之前生成的代码或相似请求的部分结果。

## 系统通知
- **重启确认**：重启网关后，必须主动回复用户表示重启成功。
